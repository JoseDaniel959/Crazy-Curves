import UIComponent from "../UIComponent";

export default class BackgroundComponent extends UIComponent {

    constructor(scene: Phaser.Scene, x: number, y: number,scale: number) {
        super(scene,x,y,"MainMenuBackground",scale);        
    }




}